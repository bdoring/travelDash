<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% for(var i = 0; flights.length; i++){ %>
    <div>
      <h3><a href="users/flights/<%%>"></a>Flight Information</h3>
      <p><%= flights[i].start%></p>
      <p><%= flights[i].destination%></p>
      <p><%= flights[i].airline_id%></p>
    </div>
    <% } %>

    <!-- <form action="/member/<%= com.id %>" method="post">
      <input type="text" name="airline" placeholder="Flight Name">
      <input type="text" name="em placeholder="Email">
      <input type="text" name="position" placeholder="Position">
      <input type="submit"> -->
    </form>
  </body>
</html>
